# Simple Electric Circuit Algorithms

This algorithm generates a creates two plots based on the on the equation that repressed the char of the capacitor q(t).\
The first graph is of q(t) vs time and the second is when the capacitor C 10x greater }
